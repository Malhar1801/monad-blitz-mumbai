/**
 * seedChallenges.js — No longer needed on-chain.
 *
 * Challenges now live in challenges.json (off-chain, backend only).
 * ChallengeRegistry contract has been removed from the architecture.
 *
 * To add or modify challenges: edit challenges.json in the project root.
 * No deployment or seeding step required.
 */

console.log('Challenges are now managed via challenges.json (off-chain).');
console.log('ChallengeRegistry contract has been removed.');
console.log('Edit challenges.json directly to add/modify challenges.');
