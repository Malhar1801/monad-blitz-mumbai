# ── AI (Google Gemini — free tier) ───────────────────────────────────────────
# Get key at: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=AIza...

# ── IPFS (Pinata) ─────────────────────────────────────────────────────────────
PINATA_API_KEY=
PINATA_SECRET=

# ── Blockchain (Monad Testnet) ────────────────────────────────────────────────
# Deployer wallet private key (no 0x prefix)
PRIVATE_KEY=969d091dbb23498fc894d7ae5b174682492a2ccceb58603ff2d20595e89ba7e1
MONAD_RPC_URL=https://testnet-rpc.monad.xyz

# ── Database ──────────────────────────────────────────────────────────────────
# MongoDB — used as vector store for prompt embeddings
MONGODB_URI=mongodb://localhost:27017/monad

# ── Server ────────────────────────────────────────────────────────────────────
PORT=5000

# ── Embeddings ────────────────────────────────────────────────────────────────
# No API key needed — Xenova/all-MiniLM-L6-v2 runs fully locally.
# Model (~22 MB) is auto-downloaded to .model-cache/ on first run.
